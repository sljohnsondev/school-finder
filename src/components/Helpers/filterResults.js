const filterResults = (filterData, schoolsData) => {

  const { gradeLevel, schoolType, carMode, publicMode, walkMode, commuteDist, commuteTime, viewFilters, homeAddress } = filterData;
  let finalResults = [];

  for (i = 0; i < filterData.length; i++) {
    
  }
}

export default filterResults;
